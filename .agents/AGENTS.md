# Git Usage Rules

- Do NOT execute `git push` on behalf of the user, as their git configuration requires a password/authentication step that the agent cannot fulfill. 
- Only use `git add` and `git commit` to stage and commit changes.
- Remind the user to run `git push` manually when needed.
